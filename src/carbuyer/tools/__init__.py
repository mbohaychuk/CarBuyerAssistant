"""Operator utilities (one-shot scripts, setup helpers).

Each module in this package is runnable as `python -m carbuyer.tools.<name>`.
These are not part of the production worker fleet — they're for setup,
debugging, and ad-hoc data inspection.
"""
